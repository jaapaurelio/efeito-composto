import React from "react"
import { myContext } from "./provider"
import Slider from "rc-slider"
import currencyFormatter from "currency-formatter"

export default function Interest({ selector, payment, value, total }) {
  return (
    <myContext.Consumer>
      {context => <React.Fragment>###</React.Fragment>}
    </myContext.Consumer>
  )
}
